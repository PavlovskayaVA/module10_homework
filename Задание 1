let value=+prompt('Введите значение: ', 'Ваше значение');
if (isNaN(value % 2) || value == ' ')
  {
    alert('Упс, кажется, вы ошиблись.');
  }
      
else
  {
    if(value%2==0)
      {
         alert('Значение четное');
      }
    else
      {
         alert('Значение нечетное');
      }
  }
